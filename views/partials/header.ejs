<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <title>Document</title>
</head>
<body>
    

<header>
  <% if (!locals.result && !locals.error) { %>
    <h1 class="title">Welcome to Cloud Pal</h1>
  <% } %>     

  <div class="container background"></div>

  <% if (!locals.result && !locals.error) { %>
    <!-- Show form only when no result and no error -->
    <div class="containera">
      <div class="inner-cont">
        <h2 style="text-align: center;">Where are you today?</h2>
        <form action="/check" method="post">
          <label for="city"><b>City:</b></label>
          <input list="cities" id="city" name="city" placeholder="Start typing a city..." required>
          <datalist id="cities">
            <!-- Populate dynamically if desired -->
          </datalist>
          <input type="submit" value="Submit" class="button">
        </form>
      </div>
    </div>
  <% } else if (locals.error) { %>
    <!-- Show error instead of form -->
    <div class="containera">
      <div class="inner-cont">
        <h2 style="text-align: center; color: inherit;">Error</h2>
        <p style="text-align: center; font-weight: bold; color: inherit;">
          <%= error %>
        </p>
        <form action="/" method="get">
          <button type="submit" class="buttona">Try Again</button>
        </form>
      </div>
    </div>
  <% } %>
</header>


